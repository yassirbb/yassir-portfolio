export const breakpoints = {
  "--viewport-wide": "(width <= 1180px)",
  "--viewport-desktop": "(width <= 1100px)",
  "--viewport-grid": "(width <= 900px)",
  "--viewport-tablet": "(width <= 820px)",
  "--viewport-content": "(width <= 760px)",
  "--viewport-compact": "(width <= 680px)",
  "--viewport-mobile": "(width <= 520px)",
  "--viewport-narrow": "(width <= 440px)",
  "--reduced-motion": "(prefers-reduced-motion: reduce)"
};
